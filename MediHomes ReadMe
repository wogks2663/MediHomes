MediHomes 프로젝트
- MediHomes는 질병과 의료기기 보유 상황에 맞추어서 사용자에게 필요하고 좋은 병원을 손쉽게 찾을 수 있도록 서비스 제공
- 증상을 가지고 질병을 추측하여 진료과를 정하거나 병원을 선택할 때 도움 목적

* MediHomes폴더는 Spring으로 MVC구현 
* self_test폴더는 python을 이용해서 자가진단 머신러닝 구현

실행순서
1. 두개의 폴더를 모두 다운 받아 각각 import한다. 
2. self_test폴더에서 selftest.py를 실행한다.
3. MediHomes폴더에서 src/main/resources -> SQL -> create_db, table.sql을 mysql을 이용해 db와 table을 만든다.
4. 만든 table에 각각 csv파일을 import시킨다.
5. spring에서 controllor로 실행 후, 주소에 http://localhost:8080/project/mediMain을 입력한다.
6. 홈페이지를 이용해본다
